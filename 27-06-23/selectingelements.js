const el = document.querySelector("h1");

el
el.classList
el.className
el.id
el.innerHTML


const button1 = document.querySelector('button');


button1.addEventListener('click',() => {

    alert("you are sure to submit");
})

button1.getBoundingClientRect() // details

// attribute checker

button1.hasAttribute('true');

// attribute remover

button1.remove.Attribute('true');
