// object

// in another that type we can store only one value 4

// but in object we can put the multiple values

// object

const person = {
    name:'john',
    age: 25,
}

console.log(person);

// if you want the perticular element in object so we use dot

console.log(person.age); 

// below line show the type of person that is objet
console.log( typeof person);

// but we 
console.log( typeof person.name);
